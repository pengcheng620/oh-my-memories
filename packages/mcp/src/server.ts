// MCP server entry. M1.1 — placeholder.
// Will use @modelcontextprotocol/sdk to expose recall_across_sources / scan_sources / migrate
// as tools over stdio. Deferred from M1 per research/G-skill-vs-mcp.md: ship CLI + Skill first,
// freeze the CLI I/O contract, then wrap as tools to avoid double breaking-change cost.

export function placeholder(): void {
  // intentional empty
}
