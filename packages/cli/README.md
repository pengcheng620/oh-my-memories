# @oh-my-memories/cli

The `omem` CLI — main entry for humans and AI agents.

```bash
npm install -g oh-my-memories
omem --help
```

See repo root [`README.md`](../../README.md) for product overview and [`AGENTS.md`](../../AGENTS.md) for design context.
