# @oh-my-memories/adapter-serena

Serena MCP adapter — reads project-local `.serena/memories/*.md`.

Serena memories are per-project (not global), so this adapter takes a `projectRoot` in its constructor.
